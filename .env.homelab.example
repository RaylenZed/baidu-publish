# docker-compose.homelab.yml 使用的变量

# 对外暴露端口
BACKEND_PORT=18000
FRONTEND_PORT=15173

# 前端构建时注入的 API / WebSocket 地址（DDNS 域名 + 端口）
# 示例：myhome.ddns.net
VITE_API_BASE_URL=http://myhome.ddns.net:18000/api/v1
VITE_WS_BASE_URL=ws://myhome.ddns.net:18000
